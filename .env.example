# Database Configuration
# For local development, set up a PostgreSQL database and add the connection string here
# Example: postgresql://username:password@localhost:5432/wavesdb
DATABASE_URL=

# Node Environment
NODE_ENV=development

# Server Port (default: 5000)
PORT=5000
